
# Instructions

Compile then run:

    ./load_bad_data

Confirm that nestedOne and nestedTwo have the wrong values (nestedOne has value 2
and nestedTwo has value 1). If the types were different it would result in a crash.

`./load_good_data` loads the same data with consistent nested field orderings.
